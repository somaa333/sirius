# `constants/`

Shared **non-secret** values used across features.

- **`analysisProgress.js`** — Step labels and simulated progress cap for the analysis progress modal (must stay aligned with `AnalysisPage` simulation).

Environment-specific values belong in **`.env`** (see root `.env.example`).
