# `hooks/`

Reserved for shared React hooks. Custom hooks that are not yet extracted from pages/components may still live next to their feature code.

When adding a hook, prefer a `useXxx` name and keep side effects and API calls easy to test.
