# `styles/`

Global styles live in **`src/index.css`**. Feature and page styles are mostly **co-located** next to components (`*.css` beside `*.jsx`) or under `pages/`.

This folder is reserved for future shared partials (e.g. tokens) if the team centralizes CSS variables.
