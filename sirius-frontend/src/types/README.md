# `types/`

This project is primarily **JavaScript** with **JSDoc** for documentation. Domain types are often described inline in `data/` and `services/` using `@typedef` and `@param` tags.

If you add **TypeScript** later, shared interfaces can live here (e.g. `assessment.d.ts`).
